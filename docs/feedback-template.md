# Feedback Summary Template

Fill in after collecting real feedback via the in-app form (`/feedback`, `POST /api/v1/feedback`).

- **Number of testers:**
- **Average overall rating:**
- **Average usability rating:**
- **Average trust rating:**
- **Common positive feedback:**
- **Common problems reported:**
- **Changes made after testing:**
- **Future requests:**

Pull raw numbers from `GET /api/v1/admin/stats` and `GET /api/v1/admin/feedback` — do not invent figures.
